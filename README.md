# Modern Flask Clock

Simple, modern-looking clock website built with Flask.

Run locally:
```
python -m venv venv
source venv/bin/activate  # or venv\Scripts\activate on Windows
pip install -r requirements.txt
python app.py
```
Open http://127.0.0.1:5000
